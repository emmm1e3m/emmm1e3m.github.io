# 旅行饼狗使用说明

## V8 的核心规则

- 新开始的旅行、刷播、冲热、弹琴和睡觉默认读条均为 10 秒，开始前会在右侧信息栏二次确认。
- 五类饼屋活动每成功领取一次，或苹果钟完成专注与休息整轮，陪伴天数增加一天。提前取消、重复检查、普通现实停留结算和待办操作不会进入下一天；活动取消也不会返还已经消耗的补给或幸运苹果。
- 今日 Bingo 三项全部完成后会以 `√` 保留，等下一次游戏日（陪伴日）推进时再刷新；任一任务尚未完成，则跨游戏日继承原任务与完成状态。成功领取任一种饼屋活动，或完成一轮完整苹果钟，都会推进一个游戏日。
- 新任务会先检查当前收藏是否足以完成：没有对应收藏时不会出现“重看明信片”“重看百万瞬间”或“重看全站第一”，少于两份有效收藏时不会要求重温两份回忆。读取缓存/本地存档或在 DEBUG 中移除收藏后，已经无法完成但结构合法的未完成任务会稳定换成可完成任务；已完成任务和仍可完成任务的进度不变，损坏或篡改的任务板仍会被拒绝。
- 两款游戏字体已扩展为 2500 个标准常用汉字加当前界面表外用字；以后输入或新增普通常用汉字文案时，不会再因为旧字体只含当时页面字符而突然回退到系统字体。
- 存档会记录正在进行的活动及其绝对开始、结束时间和奖励计划。即使以后再次修改默认时长，旧任务仍按开始时写入的时间完成。
- 饼狗对旅行、电脑和音乐分别有自己的意愿；刷播与冲热共享电脑意愿。它不想做的入口会变暗，点击只显示原因，不会偷偷开始或扣资源。
- 点击房间空白会显示当前任务和饼狗兴趣概览。活动中左下 `↩️` 会打开真正的取消确认，右上 `ℹ️` 打开当前维度说明，右下 `🔃` 会先介绍并确认再切换维度。

## 冰箱与两种魔法

冰箱共有七种带 emoji 的道具，金额统一显示为 `N🍎`：

- 🍱普通旅行便当、🍎苹果旅行便当、🎧信号耳机、🧰热度工具箱和 🍀幸运苹果；
- ⚡瓶装速度魔法：读条仍在进行时可消耗一瓶，让这次活动立刻进入可领取状态；
- ✨瓶装活力魔法：让饼狗接下来七个相伴日对旅行、电脑和音乐都有兴趣。

瓶装速度魔法单价为 2🍎，瓶装活力魔法单价为 7🍎；幸运苹果会把对应收藏事项的目标概率翻倍。

七个相伴日会由成功领取饼屋活动或完成苹果钟推进，不是七个自然日。苹果钟推进到第七天时，活力效果结束并恢复饼狗当日偏好。魔法不需要时不会被消耗。

## 收藏与好朋友

- 明信片和百万直拍随机从未拥有项中选择，不会重复；百万直拍基础概率为 30%，使用幸运苹果后为 60%。全站第一基础概率为 15%，按 Dynamite → Talk WORTHY? Talk DIRTY! → Make A Wish → Dawn in my soul → 愿与愁 → 囍 → Hey Mama → POWER 的时间顺序获得。
- 旅行以 10% 基础概率遇见朋友，命中时不会同时得到明信片，并收到固定道具。课代饼、三好兔、心好兔、信号狗、饼哩饼哩在旅行中依次赠送 2/3/4/3/2🍎；已经认识的朋友仍可再次遇见并照常赠礼。弹琴只会召回已经认识的朋友，不会凭空解锁新朋友；每位已认识朋友增加 15% 来访概率，五位朋友依次赠送 4/6/8/6/4🍎，所以认识的朋友越多，每次弹琴的期望赠礼越高。
- 收藏墙只展示已经获得的明信片、百万直拍、全站第一和好友。没有内容时显示“惊喜会在相遇时悄悄出现。”
- 卡片缩略图可以铺满裁切；收藏详情、全屏预览和明信片选择器会以 `contain` 完整显示并允许留白，苹果钟运行背景则以 `cover` 铺满并允许裁切。在详情中点击收藏图片，可以全屏查看并下载到本地。
- DEBUG 的“一键全收集”包含好友，“清空收集”会同时清空收藏与好友记录；无需维护专用全收集存档。

存档不写死当前收藏或好友总数，也不保存分类解锁状态。以后增加内容时，旧存档会把新增 ID 视为尚未获得的内容。

## 房间、电子琴与播放器

- 房间严格使用用户提供的 1098 × 1433 透明原图，网页不重绘、不拉伸、不裁切，只生成等比无损 WebP，并以 `contain` 完整显示；宽屏横向房间区域允许保留透明侧边留白。饼狗在床、两台电脑、衣架、电子琴、冰箱、唱片机、收藏墙和房门的位置都按原图像素坐标锁定。
- 电子琴有 C3–B6 四个完整八度，共 48 个半音，从上到下显示 C6、C5、C4、C3。鼠标、触摸可以弹全部琴键；物理键盘白键从高到低依次映射 `1–7`、`Q–U`、`A–J`、`Z–M`。
- 唱片机固定提供八首全站第一，从 Dynamite 到 POWER 按时间顺序排列，不提供自定义曲库。列表、单曲和随机模式都可使用上一首、下一首；单曲模式下主动切歌仍会移动到相邻曲目。
- 唱片机与收藏详情共用一个持久播放器。选曲或打开视频详情会从头请求自动播放；画面展开时可直接操作 B 站播放器，收起后画面不可操作，也可用游戏控件显示/隐藏画面、暂停/继续或取消播放。若直接在 B 站画面里暂停或拖动，游戏无法读取这个跨站变化，估算计时不会同步；切换信息内容、收藏墙或维度后不会意外中断。
- 存档只记住当前曲目和循环模式；重新打开页面不会恢复正在播放的进度。

## 现实生活维度

PC 浏览器可通过房间右下角切换维度；手机、平板等没有精细指针能力的浏览器不能进入现实维度：

- 二楼电脑拆分为“刷播”和“冲热（开发中）”；冲热页提供居中的“前往字母建设站”入口。
- 刷播只允许额外填写一个自测视频 BV 号，也可以留空；输入完整有效后会立即跟随存档。其余视频来自本站在构建期从 `fid=3963921644` 保存的同源静态快照，游玩时不会访问 B 站收藏夹 API，也不使用跨域代理。
- “登录刷播”会用弹出窗口或新标签页依次打开视频；实验性的“维度穿透”会在没有登录刷播时自动运行游客刷播。两者不能同时运行，切换回游戏维度后当前方式仍会继续。
- 两种方式共用自测 BV、视频间隔和轮次间隔。默认每个视频间隔 8 秒，设备或网络较卡顿时可以调到 1–60 秒；本轮最后一个页面打开或 iframe 创建 310 秒后开始下一轮。游客刷播会把本轮的隐藏播放器保留到整轮结束，再统一换成下一轮；顶栏会持续显示运行时间和轮次。
- 可按小时设置本次刷播何时停止，0 或留空表示不限时，最长 24 小时；设置启动后不会中途改变。登录时尽量不要连续刷播超过 5 小时，以降低黑号风险。
- 浏览器必须允许本站弹出窗口。关闭游戏页、点击停止或到达限时都会清理页面；后台冻结后只处理当前到期步骤。信息页显示累计轮次，最近 10 条改为按“每次启动任务”记录，一次任务包含其中的所有轮次、起止时间和完成/停止结果，0 轮任务也会留下记录。DEBUG 可以在本次页面会话把刷播轮次设为 1–3600 秒，当前轮保持原时长，从下一轮开始生效。
- 跨站窗口和游客播放器只能发出加载请求；游戏无法读取 B 站内部状态，因此不会承诺真实播放、播完或计数增加。
- 一楼电脑“工作”先显示待办，再显示已解锁明信片选择器。苹果钟提供 25+5、50+10、90+15 三组专注与休息周期。
- 苹果钟启动后把选定明信片作为 `cover` 全屏背景，并展示陪伴饼狗、待办和可操作的游戏播放器控件；完成专注与休息整轮才增加一天陪伴天数。允许通知后，阶段结束会请求系统提醒；若通知不可用或未授权，会使用页内提示。
- 每在现实维度停留完整 10 分钟，结算基数增加 1🍎。返回游戏时，选择“是的🥰”获得全额，选择“没有🥺”获得向下取整的一半；现实停留结算和待办操作本身都不增加陪伴天数。

## 存档兼容与浏览器限制

- 浏览器缓存是日常主存档。标题页在存在缓存时显示“继续”，并始终提供“全新旅程”和“本地存档”；`.bingo` 文件用于备份和跨浏览器导入。
- 通过“全新旅程”或“本地存档”覆盖缓存之前、检测到网页更新时都会先下载旧档；首次获得新的全站第一或认识新朋友时会下载关键节点存档，每隔三天也会请求一次备份。
- 新存档为 V8，版本号为 `0.8.0-demo.1`；读取时严格支持 V1–V8。系统先校验原文件摘要，再迁移和核对当前目录，不会先改文件再验摘要。
- 旧缓存成功迁移或协调后，会同步写回 V8 进度、当前游戏版本与缓存更新时间；缓存外层格式版本不随业务存档版本改变。历史 V6→V7 迁移仍把每条旧刷播轮次独立转换成一条旧任务记录，不按时间猜测合并；V7→V8 默认使用空自测 BV 并关闭维度穿透。
- V8 存档包含当前活动、两种魔法、现实停留与待结算奖励、待办、完整苹果钟、播放器设置、刷播累计轮次与最近 10 次任务记录，以及单个可选自测 BV 和维度穿透开关；不会复制海报、视频元数据或收藏目录总数。
- “检查更新”只保留在标题页，检查结果是临时提示；检测到新版本时会自动备份缓存进度，再提供更新安装。
- 守候音频只在进入旅程的用户手势后启动并保持开启，不在状态栏显示开关。它只能尽量减少浏览器休眠，不能绕过系统节能和后台冻结。
- 移动设备以触屏为主时，游戏会在旅程运行且页面可见期间请求屏幕常亮；隐藏页面会释放，重新显示时再请求。不支持或拒绝常亮权限不会影响继续游玩。
- 系统通知需要用户主动授权。页面完全关闭、浏览器被冻结或设备休眠时，不保证准时弹出；返回页面后会按绝对时间补检查。

## 本地启动与验证

Windows 下双击仓库根目录的 `启动旅行饼狗.cmd`，或运行：

```powershell
npm ci
npm run dev -- --host localhost --port 5173 --strictPort --open "/AllForSUXINHAO/TravellingBingo/"
```

完整本地门禁：

```powershell
npm run verify
npm run build
npm run site:assemble
npm run site:verify
npm run test:e2e
```

其中 Playwright 浏览器测试不包含在 `npm run verify` 内，需要单独执行最后一条命令。
