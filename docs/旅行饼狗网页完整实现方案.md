# 旅行饼狗网页完整实现方案

> 原始需求：[旅行饼狗开发纲要](specs/旅行饼狗开发纲要.md)
>
> 固定路径：`https://emmm1e3m.github.io/AllForSUXINHAO/TravellingBingo/`

## 1. 产品定义

“旅行饼狗”是一款以饼狗为主角的离线收集网页游戏。玩家在铲铲饼屋陪饼狗旅行、刷播、冲热、弹琴和睡觉，通过读条、Bingo 小任务、收藏与好友积累陪伴回忆；也可以切换到现实生活维度，用苹果钟和待办让游戏陪伴真实工作与学习。

产品原则：

- 房间是视觉主体，信息栏只解释当前语境与可执行动作。
- 饼狗有自己的旅行、电脑和音乐意愿；拒绝是可见状态，不是隐藏入口。
- 五类饼屋读条共享二次确认、绝对时间、取消、领取和存档恢复规则；现实苹果钟使用独立的绝对时间完成流程。
- 🍎 的获得与花费都有明确来源；金额一律显示为 `N🍎`。
- 收藏与好友以第一次相遇为惊喜，不提前展示灰色占位、未解锁分类或永久总数。
- 游戏状态与第三方播放器、浏览器通知等外部能力严格分层；不可观测的外部状态不得被伪装成成功。
- `.bingo` 由玩家自己保管，不上传、不登录。

## 2. 运行与公开边界

- 游戏固定部署在 `/AllForSUXINHAO/TravellingBingo/`；根首页只提供入口。
- Vite `base`、PWA `id`、`start_url`、`scope` 与导航回退均使用该子路径。
- 游戏不需要后端、数据库、排行榜、支付、广告、统计或用户跟踪。
- 首次在线访问并缓存应用外壳后，标题页、房间、计时、领奖和存档可离线使用；第三方 B 站 iframe 与未缓存高清图不承诺离线可用。
- 运行时不调用 B 站收藏夹 API，也不跨域抓取微博图片。内容目录在构建期固化为同源 JSON。
- Pages 只上传隔离组装的 `_site/`，不公开 `docs/`、`research/`、`resources/raw/`、源码、浏览器状态或抓取凭证。

## 3. 主流程

```text
根首页入口
  → 标题页
      ├→ 显式检查新布置
      ├→ 输入称呼并开始新旅程
      └→ 选择 .bingo → 摘要预览 → 确认导入
  → 铲铲饼屋
      ├→ 房间空白 → 当前任务与意愿概览
      ├→ 设施 → 对应信息栏
      ├→ 读条活动 → 二次确认 → running / ready
      │    ├→ ↩️ 取消：不计天、不退款、不发奖
      │    ├→ 速度魔法：立即 ready
      │    └→ 领取：陪伴天数 +1
      ├→ 收藏墙 → 收藏 / 好朋友 / 视频详情
      ├→ 唱片机 → 持久播放器
      ├→ 电子琴 → 三排琴键
      ├→ ℹ️ → 简洁的“铲铲饼屋的小纸条”
      └→ 右下维度按钮
           ├→ 游戏维度
           └→ 现实维度 → 数据 / 工作
                ├→ 苹果钟首次到点完成：陪伴天数 +1
                └→ 返回结算：只结算 🍎，不加天
  → 离开房间
  → 下载最新 .bingo 并确认保存
  → 返回标题页
```

## 4. 标题页、更新检查与称呼

- 标题页提供“开始新旅程”“读取 .bingo 存档”和显式“检查新布置”。
- 称呼去除首尾空白，接受 1–16 个非空 Unicode 字符；不合格时在输入框附近说明。
- 导入预览显示文件名、导出时间、游戏版本、称呼、陪伴天数、`N🍎`、收藏数量和当前活动。
- 文件选择只创建待确认预览，不立即覆盖内存状态。
- “检查新布置”调用当前 Service Worker registration 的 `update()`；没有 Service Worker、尚未注册或网络失败时显示相应状态。
- 检查不等于强制刷新。新 Service Worker 可用时仍要经过未保存进度保护。
- 连续激活标题五次后可输入 `TravellingBingo` 解锁 DEBUG；它是隐藏入口，不是安全边界。

## 5. 视觉、字体与通用文案

### 5.1 字体

| 用途                                   | 字体       | 公开文件                              |
| -------------------------------------- | ---------- | ------------------------------------- |
| 游戏标题、页面大标、房间设施、收藏分类 | 可画乐融融 | `assets/fonts/kehua-lerongrong.woff2` |
| 正文、按钮、任务、日期与小型 UI        | 可画奶糖体 | `assets/fonts/kehua-naitang.woff2`    |

字体源位于 `resources/raw/travelling-bingo/fonts/`，公开页面只发布项目字符子集 WOFF2。日期中的“月”等汉字必须包含在子集并使用同一正文字体。

### 5.2 货币与固定文案

- 可见金额统一为 `N🍎`，例如 `3🍎`、`还差 2🍎`、`朋友送来 3🍎`。
- 不显示“X 个苹果”“补充·X个苹果”“按钮仍然可以按”等元叙述。
- “苹果”文字只用于角色口味、道具名称和“今天也要好好吃苹果”等自然语句。
- 冰箱标题为“补充道具”，不重复“为冰箱”。
- 衣架正文为“什么样的搭配最合适呢？”。
- 刷播说明为“在电脑前好好陪伴，加速百万直拍的达成。”。
- 空收藏墙文案为“惊喜会在相遇时悄悄出现。”。

### 5.3 控件

- 触控目标至少 `44 × 44 CSS px`，具备焦点圈、按压反馈和可理解的可访问名称。
- 房间设施用文字标签，不要求玩家猜抽象图标。
- 顶栏中心“今天也要好好吃苹果”单行真正居中；各 HUD 文字使用同一字号等级。
- 信息栏顶行左侧是语境标签，右侧是“收起信息栏”；活动进度语境使用“这一次 Bingo”。
- 设施信息使用常规左对齐的读条卡层级，不复用大型居中结果页布局。
- `ℹ️` 只打开上一版短小的说明面板，角标精确写“铲铲饼屋的小纸条”；不得扩写成长篇设施或价格手册，详细价格只在冰箱展示。

## 6. 房间母版与布局

### 6.1 唯一母版

```text
resources/raw/travelling-bingo/generated/chan-chan-house-master.png
```

母版为用户提供的 1098 × 1433 RGBA 原图。应用目录 `AllForSUXINHAO/TravellingBingo/` 下的公开派生图沿用兼容文件名：

```text
public/assets/game/chan-chan-house-v2-768.webp
public/assets/game/chan-chan-house-v2-1098.webp
```

处理只允许等比缩放和无损 WebP 编码；不得裁切、重绘、补画、改变构图或丢失透明通道。

### 6.2 桌面与移动布局

- 顶栏、房间和信息栏共享页面 inset。房间左侧与顶栏左侧对齐，信息栏右侧与顶栏右侧对齐。
- 房间到信息栏的横向 gap 与顶栏到内容的纵向 gap 使用同一紧凑 token。
- 收起信息栏时房屋可以占据可用宽度；再次点击空白会直接打开状态概览。
- 房屋与信息栏不做展开/收起动画；切换不同信息内容时使用轻量 keyed 入场动画。
- 手机上信息栏变为单列全宽内容；关闭、取消、维度切换和播放器控制必须留在视口或可滚动范围内。
- 隐藏滚动条不等于禁止滚动；滚轮、触摸、方向键和 PageUp/PageDown 仍要工作。

### 6.3 饼狗中心点

所有中心点以母版左上角为原点：

| 位置         |    X |    Y |
| ------------ | ---: | ---: |
| 床上         |  225 |  300 |
| 二楼电脑前   |  504 |  409 |
| 衣架前       |  387 |  675 |
| 电子琴前     |  257 | 1103 |
| 一楼电脑前   |  420 | 1172 |
| 冰箱前       |  633 |  951 |
| 唱片机前     |  783 | 1030 |
| 收藏墙前     | 1053 |  673 |
| 门口         |  980 | 1176 |
| 默认房间中央 |  620 | 1180 |

`roomConfig.ts` 保存像素中心点，渲染时统一除以 1098 / 1433 换算百分比。设施热点继续使用既有位置，不与角色中心点混写。旅行 `outside` 状态不渲染饼狗 DOM。

## 7. V4 领域状态

精确类型以 `src/domain/game/types.ts` 为准，核心形状为：

```ts
interface GameStateV4 {
  schemaVersion: 4
  profile: {
    createdAt: number
    debug: boolean
    displayName: string
    companionDays: number
  }
  economy: { apples: number }
  inventory: Record<ItemId, number>
  collections: Record<string, CollectionEntry>
  friends: Partial<Record<FriendId, FriendEntry>>
  activeActivity: ActivityRun | null
  pet: PetState
  tasks: TaskBoard
  gameBalance: GameBalance
  statistics: GameStatistics
  random: PersistentRandomState
  world: 'game' | 'reality'
  player: {
    effects: { vitality: VitalityEffect | null }
  }
  reality: {
    nextStaySequence: number
    activeStay: RealityStay | null
    pendingSettlement: RealitySettlement | null
    todos: Record<string, TodoItem>
    pomodoro: PomodoroState
  }
  musicPlayer: MusicPlayerState
}
```

`ActivityRun` 保存 `runId`、种类、`startedAt`、`endsAt`、`rewardSeed`、完整 `rewardPlan`、补给和幸运苹果快照。当前面板、弹窗、琴键按下状态、奖励 UI、走路和日夜动画不写入存档。

### 7.1 目录不是存档

存档不保存：

- 收藏分类总数、总收藏数或完成百分比；
- 全站第一的“下一项”索引；
- 分类解锁数组；
- 好友总数与图片元数据；
- B 站标题、作者、海报或视频目录副本。

完成度、可见分类、下一项全站第一和播放器展示信息均由“当前目录 + 存档 ID”派生。目录增加项目时旧档无需重置。

## 8. `.bingo` 导入、导出与迁移

### 8.1 外层格式

- UTF-8 JSON，扩展名 `.bingo`，MIME `application/octet-stream`。
- 外层包含格式标识、封套版本、`gameVersion`、导出时间、业务 payload 与 SHA-256 摘要。
- 最大 1 MiB；未知字段由严格 Zod schema 拒绝。
- SHA-256 只用于发现损坏或误改，不是签名或加密。

### 8.2 V4 导出

- 新导出只写 `schemaVersion: 4`，`gameVersion: 0.4.0-demo.1`。
- 用户名、天数、活动、任务、随机序列、收藏、好友、七种道具、魔法、现实、待办、苹果钟、播放列表与播放器设置完整往返。
- 正常文件名为 `travelling-bingo-YYYYMMDD-HHmmss.bingo`；DEBUG 在扩展名前加 `-debug`。
- 未导出变更注册 `beforeunload`。离开房间或安装 PWA 更新使用两阶段保存确认；确认期间出现新 revision 时旧确认失效。

### 8.3 V1–V4 导入顺序

1. 先按文件原始 payload 校验摘要。
2. 用冻结的严格 V1、V2、V3 或 V4 schema 解析。
3. 逐代迁移到 V4。
4. 规范非 DEBUG 状态未来活动的默认平衡。
5. 用当前目录协调可安全修复的旧引用。
6. 最终语义校验通过后才允许确认进入。

迁移不变量：

- 旧活动的 `startedAt`、`endsAt`、`rewardSeed`、`rewardPlan` 原样保留。
- V4 默认 72 秒只影响迁移之后新开始的活动。
- 历史用户名缺失时使用兼容称呼；陪伴天数由旧领取统计推导。
- V3 活动、好友与意愿字段保留；V4 新魔法、现实与播放器字段使用安全初值。
- V4 不允许退役的 `greet-bingo` 留在当前任务板；迁移按当前任务目录替换，并保持任务 PRNG 可重现。
- 已失效的苹果钟明信片背景可以清空；非法奖励组合、未知活动奖励或篡改时间必须拒绝，不能被协调步骤静默删除。

## 9. 五类读条与陪伴天数

```ts
BASE_ACTIVITY_DURATION_MS = 72_000
```

| 活动 | 入口     | 意愿     | 补给       | 位置     | 主要结果                |
| ---- | -------- | -------- | ---------- | -------- | ----------------------- |
| 旅行 | 房门     | 旅行     | 旅行便当   | 房外     | 好友道具或明信片        |
| 刷播 | 二楼电脑 | 电脑     | 信号耳机   | 电脑前   | 百万直拍                |
| 冲热 | 二楼电脑 | 电脑     | 热度工具箱 | 电脑前   | 全站第一                |
| 弹琴 | 电子琴   | 音乐     | 无         | 电子琴前 | 已认识好友可能赠 2–4🍎  |
| 睡觉 | 床铺     | 始终允许 | 无         | 床上     | 1🍎、刷新意愿、日夜转场 |

- 五类开始前都显示时长、消耗、可能结果和取消语义。
- `endsAt - 1 ms` 仍是 running；从 `endsAt` 起为 ready。
- 计时视图每秒刷新，页面重新聚焦或可见时按系统时间重算；长 `setTimeout` 不是权威时钟。
- 每次成功领取当前 `runId`，`companionDays` 恰好 `+1`。
- 取消必须携带当前 `runId`，避免旧确认清除新任务。
- 取消不计天、不发奖、不退款，也不回滚开始活动时推进的奖励随机序列。
- 陪伴天数的另一来源是现实苹果钟首次到点完成；普通现实停留结算和待办不属于陪伴日。

## 10. 意愿、状态栏与两种魔法

- 意愿键为 `travel | computer | music`；刷播和冲热共享 `computer`。
- 每轮至少有一组非睡眠活动可做；睡觉领取后刷新意愿。
- 不愿意入口视觉变淡并带 `aria-disabled`，但保留点击以显示拒绝说明或活力魔法确认。
- 顶部状态栏显示当前活动、ready、疲劳或活力剩余相伴日，并提供守候音频和检查新布置。

### 10.1 瓶装速度魔法

- 道具 ID：`bottled-speed-magic`，价格 8🍎，emoji ⚡。
- 仅当前活动处于 running 时可用。
- 二次确认后消耗一瓶，将当前活动 `endsAt` 原子改为使用时间；奖励计划不变。
- 已 ready、runId 不匹配、没有道具或时间非法时不消耗。

### 10.2 瓶装活力魔法

- 道具 ID：`bottled-vitality-magic`，价格 12🍎，emoji ✨。
- 使用后把旅行、电脑、音乐意愿全部设为真并清除疲劳。
- `activatedOnCompanionDay` 记录生效日，`expiresAfterCompanionDay` 为其后第七个相伴日的 exclusive 边界。
- 领取饼屋活动或首次完成到点苹果钟都会推进相伴日。有效期间睡觉刷新不能把意愿改回 false；第七日由相应完成动作清除 effect，并按持久偏好序列恢复当日意愿。
- 已有有效效果、库存不足、达到天数上限或当前无需使用时拒绝且不消耗。

## 11. Bingo 与 🍎 经济

- 新游戏和迁移后都保持三条活动任务；同一板不重复模板。
- 单条完成立即发放任务快照中的 🍎，三条全部完成后原子刷新新板。
- 旧板最后一个事件不能继续推进新板。
- V4 不再签发“和饼狗打个招呼”；可验证事件包括房间走访、收藏查看、琴键、唱片机、收藏播放器和舞台测试。
- 唱片机与收藏播放器事件只代表用户主动请求对应 BV，不能代表 iframe 已加载、播放或播完。

经济流：

```text
Bingo 小任务 ───────────────────→ 🍎
睡觉完整读条 ──────────────────→ 1🍎
弹琴完整读条 + 已认识好友来访 ─→ 2–4🍎
现实维度完整十分钟 ───────────→ 结算基数 1🍎
🍎 ───────────────────────────→ 七种冰箱道具 / 明确的意愿鼓励
```

| 道具           | 价格 | 用途                     |
| -------------- | ---: | ------------------------ |
| 🍱普通旅行便当 |  3🍎 | 启动旅行                 |
| 🍎苹果旅行便当 |  5🍎 | 启动旅行，遇友概率 +0.15 |
| 🎧信号耳机     |  4🍎 | 启动刷播                 |
| 🧰热度工具箱   |  7🍎 | 启动冲热                 |
| 🍀幸运苹果     |  6🍎 | 当次对应收藏概率 +0.20   |
| ⚡瓶装速度魔法 |  8🍎 | 立即完成当前读条         |
| ✨瓶装活力魔法 | 12🍎 | 七个相伴日全部意愿为真   |

## 12. 概率、好友与收藏顺序

```ts
DEFAULT_GAME_BALANCE = {
  activityDurationMs: 72_000,
  probabilities: {
    postcard: 0.65,
    millionShot: 0.4,
    siteFirst: 0.1,
    travelFriend: 0.2,
    musicFriend: 0.2,
  },
}
```

- 所有判定使用 `roll < chance`；0 永不命中，1 必定命中。
- 幸运苹果只影响当次收藏概率，苹果旅行便当只影响旅行遇友概率，均封顶 1。
- 已集齐分类或基础概率为 1 时，不能浪费幸运苹果。

旅行顺序：

1. 判定 `travelFriend`。
2. 命中后选择好友，签发固定道具礼物并停止。
3. 没命中好友时才判定明信片。
4. 命中明信片后从未拥有项随机选择。

音乐好友：

- 只读取活动开始时已经认识的好友集合。
- 空集合时不判定来访。
- 命中后从已认识好友选择一位，按身份固定赠 2–4🍎。
- 旅行好友赠道具，不赠 🍎；音乐来访更新累计赠礼。

收藏获取：

- 明信片：`catalog.postcard - ownedIds` 中随机选择。
- 百万直拍：`catalog['million-shot'] - ownedIds` 中随机选择。
- 全站第一：`siteFirstChronology` 中第一个未拥有 ID。
- 当前 chronology 为 Dynamite → Talk WORTHY? Talk DIRTY! → Make A Wish → Dawn in my soul → 愿与愁 → 囍 → Hey Mama → POWER。
- POWER 只是当前第八项；schema 允许以后追加。

## 13. 收藏墙与详情

当前静态内容快照：

- 100 张 Bilibili Toy 明信片；
- 30 张百万直拍；
- 8 项全站第一；
- 5 位好友；
- 百万直拍和全站第一均带静态 B 站视频元数据。

显示规则：

- 没有内容时只显示空状态“惊喜会在相遇时悄悄出现。”。
- 首次获得或相遇后才显示对应分类；只渲染已拥有卡片。
- 收藏按 `firstObtainedAt` 新到旧稳定排序；未全收集时不显示总数、百分比或剩余数量。
- 好友卡使用角色图片 `cover`，展示相遇信息，不显示“收到 X🍎”。
- 所有缩略图和普通详情媒体使用 `cover`；点击图片后进入 `contain` 完整预览并提供本地下载。
- 详情外框在桌面和手机横屏为 16:9，手机竖屏为 9:16；文字区内部可滚动且隐藏可见滚动条。
- 视频详情挂载后立即请求持久播放器，不显示“打开/关闭播放器”按钮。

## 14. 三排电子琴

- 音域为 C4–B6，三个完整八度、36 个半音；三排分别对应四、五、六组，中排 C 为 C5。
- 物理键盘只映射白键：
  - C4–B4：`Z X C V B N M`
  - C5–B5：`A S D F G H J`
  - C6–B6：`Q W E R T Y U`
- 鼠标与触摸可触发全部白键、黑键；可同时按多个键。
- Web Audio 使用多谐波与衰减包络模拟钢琴音色，首次用户交互后才创建/恢复上下文。
- 读条活动与琴键交互分离；弹琴活动 running 时仍可演奏。
- 精确提示为：

```text
从 A5 G5 G5 B5 G5 E5 G5 G5 B4 C5，D5 C5 A5 A5 A5 B5 B5 B5 B5 C6 开始试试
```

## 15. 持久 B 站播放器与列表

### 15.1 静态目录

- 百万直拍收藏夹 ID：`1130054521`。
- 全站第一收藏夹 ID：`3489626721`。
- 唱片机内置曲目来自百万收藏夹静态最新页快照；运行时不抓取收藏夹。
- 每个收藏条目从自身 `metadata.video` 读取 BVID、标题、作者、发布时间与来源链接，不维护第二套手写映射。

### 15.2 播放器生命周期

- `BilibiliPlayerProvider` 在游戏根部持有唯一生产 iframe。
- 选曲或详情挂载后立即请求 `autoplay=1`；iframe 允许 autoplay/fullscreen/picture-in-picture。
- 收起信息栏、关闭收藏详情或切换维度不卸载当前 iframe；玩家可隐藏画面保持连接，也可“停止并关闭”。
- 播放请求记录来源：唱片机、收藏 ID、播放列表或直接请求。

### 15.3 播放设置与列表

- V4 状态保存播放列表、顺序、当前列表、当前 BV/索引、列表/单曲/随机模式和主动设置的起播秒数。
- 解析器接受逐行 BV 号或完整 Bilibili 视频链接，规范名称、去重、列出被拒绝行；`b23.tv` 这类不含 BV 的短链接必须先展开。
- 列表模式决定玩家主动上一首/下一首时的相邻项；单曲保持当前项；随机主动选择其他项。
- 唱片机信息栏已挂载播放列表编辑器，可在内置“百万直拍精选”和已保存曲库间切换。
- 从内置精选提交名称与 BV 列表时创建并选中新列表；选中已有列表后提交会更新现有列表。列表和播放设置随 V4 存档往返。

### 15.4 跨域限制

B 站 iframe 不向父页面提供可靠的播放状态接口。网页不能确认：

- 是否真的开始自动播放；
- 是否正在暂停；
- 真实播放位置；
- 视频是否结束。

因此：

- `autoplay=1` 是请求，不是成功回执；浏览器可能要求用户在 iframe 内操作。
- 起播秒数是 URL 参数，不是父页面可拖动并实时同步的进度条。
- 不伪造自动续播；模式只影响主动切歌。
- 任务只记录“请求打开这个 BV”。
- iframe 不可用时提供带 `noopener noreferrer` 的来源页链接。

## 16. 现实生活维度

### 16.1 进入、离开与结算

- `reality/enter` 记录 `stayId` 与绝对 `enteredAt`，并把 `world` 切为 `reality`。
- `reality/leave` 使用当前时间计算 `floor((leftAt - enteredAt) / 10min)`，返回游戏并建立待决结算。
- 玩家必须回答“是的🥰”或“没有🥺”。前者获得全额，后者获得 `floor(full / 2)`。
- 结算有稳定 `stayId`；重复、过期或不匹配请求不会再次发奖。
- 现实停留和待决结算均写入存档。

### 16.2 二楼电脑：数据

固定说明：

```text
认真刷播：在该页面中直接开始刷播（无需但是建议加入运行组）
全力冲热：在该页面中启动冲热任务（需要加入运行组，与最新版插件配合使用）
```

- 两张行动卡显示“使用方法”和“具体步骤很快会补到这里。”，不宣称已经接入真实运行任务。
- “加入运行组”打开 `https://www.weibo.com/u/7878664767`，使用 `noopener noreferrer`。

### 16.3 一楼电脑：工作

- 苹果钟提供 5、25、50 分钟选项，权威时间为持久化的 `startedAt + durationMs = endsAt`。
- 可选择已解锁明信片作为背景；只存收藏 ID，不复制图片或标题。
- 待办可创建、编辑、标记完成/未完成和删除；数据写入 V4 存档。
- 苹果钟可以取消；截止时间已到时必须完成，不能利用延迟 UI 取消绕过结果。
- 运行中的苹果钟首次到点完成时原子执行 `companionDays + 1`，并推进或结束活力七相伴日；同一 session 重复 tick、未到点 tick 和截止前取消都不加天。
- 普通现实停留结算、待办 CRUD 与单独的待办到期通知不增加陪伴天数。
- 苹果钟结束或已有到期时间的待办到期时，领域层签发稳定 notification effect，并记录已签发时间避免重复。

## 17. 通知、守候音频与 PWA

### 17.1 浏览器通知

- 只在用户点击“开启完成提醒”后调用 `Notification.requestPermission()`。
- 权限为 granted 时创建带稳定 `tag` 的系统通知；不支持、拒绝或构造失败时改用页内 toast。
- 页面计时器等待最近截止时间；`focus` 与 `visibilitychange` 后立即按绝对时间补检查。
- 页面完全关闭、浏览器冻结或设备休眠时不保证准点通知；本项目没有推送服务器或后台同步来绕过此限制。

### 17.2 10 Hz 守候音频

- 首次开始新旅程或确认导入的明确用户手势中创建一个 `AudioContext`。
- 单例 oscillator 为 10 Hz，gain 为 0.01；App 生命周期内复用。
- 状态栏可关闭：增益设 0 并 suspend；重新开启时恢复 gain 与 context。
- App 卸载时停止 oscillator、断开节点并关闭 context。
- 这是保活的尽力措施，不能绕过浏览器后台冻结、系统节能或用户静音策略。

### 17.3 PWA 更新保护

- manifest 图标使用饼狗头像 PNG，包括 192、512 与 maskable 512；网页另有 favicon 32 和 Apple Touch 180。
- Service Worker 使用 prompt 更新模式。新版本可用时，不得在未保存进度存在时直接 reload。
- 保存确认记录 revision 与 intent；确认期间产生的新进度使旧确认失效。

## 18. DEBUG

- 可调未来活动时长与明信片、百万直拍、全站第一、旅行遇友、音乐好友五类概率。
- 快捷时长包括 10 秒、30 秒和默认 1 分 12 秒。
- DEBUG 不把意愿永久强制为 true；活力魔法仍是独立的游戏状态。
- “一键全收集”按当前三类收藏目录和好友目录即时写入记录。
- “一键撤销全部收集”同时清空三类收藏与好友记录。
- 两个批量动作都二次确认并报告收藏与好友的实际变更数量。
- 不维护专用全收集 `.bingo`。

## 19. 技术架构

| 层   | 选择                                  | 职责                               |
| ---- | ------------------------------------- | ---------------------------------- |
| 运行 | Node.js 24 + npm 11                   | 本地与 CI 环境                     |
| 构建 | Vite 8                                | 子路径静态构建                     |
| UI   | React 19 + TypeScript 5.9             | 页面、房间、琴、播放器、现实工具   |
| 领域 | 纯函数 reducer                        | 活动、经济、魔法、现实、任务、列表 |
| 校验 | Zod                                   | 存档与内容目录                     |
| 离线 | vite-plugin-pwa + Workbox             | App Shell 与按需高清图缓存         |
| 图像 | Sharp                                 | 无损房图、WebP、图标和素材锁定     |
| 测试 | Vitest + Testing Library + Playwright | 领域、组件与真实浏览器流程         |

应用目录 `AllForSUXINHAO/TravellingBingo/` 下的主要路径：

```text
src/
├─ app/                     # 页面状态、导入导出、通知、更新与守候音频
├─ components/              # 饼狗、电子琴、基础播放器、焦点工具
├─ content/                 # 收藏、好友与视频 schema/目录
├─ domain/
│  ├─ activities/          # 绝对时间派生
│  ├─ game/                # V4 状态、迁移、魔法、现实、播放器状态
│  ├─ player/              # 活力效果
│  ├─ rewards/             # 收藏与好友奖励
│  └─ tasks/               # 三条任务板
├─ features/
│  ├─ title/               # 新旅程、存档摘要、更新检查
│  ├─ game/                # HUD、房间、信息栏和设施
│  ├─ player/              # 持久播放器、列表解析与控件
│  ├─ reality/             # 数据、苹果钟、待办和结算
│  ├─ album/               # 收藏、好友、视频与全屏图
│  ├─ rewards/             # 结果弹窗
│  ├─ tasks/               # Bingo 任务板
│  └─ debug/               # 调试动作
└─ infrastructure/
   └─ persistence/         # `.bingo` 封套与下载
```

## 20. 内容与素材流水线

应用目录下的公开目录：

```text
public/data/postcards.json
public/data/million-shot-posters.json
public/data/site-firsts.json
public/data/friends.json
public/data/video-catalog.json
```

素材规则：

- 明信片只从 Bilibili Toy 归档生成，不用生成图代替。
- 百万直拍与全站第一的海报和 BVID 显式一一映射。
- `Survivors` 使用第二张红白纪念海报。
- 房屋母版只使用 `chan-chan-house-master.png`。
- 好友角色可使用参考图约束的生成母版，但必须保留身份、双手双脚和完整身体。
- 公开图使用 WebP 或 PNG；不发布 HEIC、源字体、研究数据、临时联系表、登录态或 sourcemap。
- 固定文件名资源由素材清单记录尺寸、字节数和 SHA-256；哈希只用于二进制重建校验。

## 21. 安全、隐私与可访问性

- 不保存凭证、微博 Cookie、GitHub PAT 或 B 站登录状态。
- 外链使用 `noopener noreferrer`；不可信文本不传给 `innerHTML`。
- 存档按严格 schema 重建白名单字段，摘要校验在迁移之前。
- 对话框打开时移入并限制焦点，Escape 关闭，关闭后恢复到仍存在的触发点。
- 颜色不是意愿拒绝、任务完成或分类解锁的唯一信息。
- 电子琴键具有音名、物理键映射和按下状态；只映射白键。
- 隐藏滚动条仍保留键盘和触摸滚动。
- 动画遵循 `prefers-reduced-motion`。
- 自动播放、通知、更新与守候音频都提供真实状态，不将请求误写为成功。

## 22. 本地开发与验证

Windows 一键入口为根目录 `启动旅行饼狗.cmd`。手动启动：

```powershell
npm ci
npm run dev -- --host localhost --port 5173 --strictPort --open "/AllForSUXINHAO/TravellingBingo/"
```

完整门禁：

```powershell
npm run verify
npm run build
npm run site:assemble
npm run site:verify
npm run test:e2e
```

`npm run verify` 包含 Prettier、ESLint、TypeScript、Vitest、视频目录与素材校验；Playwright 不在其中，必须单独运行。

E2E 至少覆盖：

- V1、V2、V3、V4 导入与 V4 导出；旧进行中活动不改 `endsAt`。
- 五类活动二次确认、running/ready 取消、速度魔法、活力七相伴日和拒绝入口。
- 九个设施中心点、床上睡觉、衣架前站立、旅行无角色 DOM。
- 空白概览、信息栏收起、顶栏、状态栏、简洁小纸条、`↩️` 与维度按钮无重叠。
- C4–B6 三排琴、白键映射、精确提示和无横向滚动。
- 收藏/好友 cover、详情 16:9 / 9:16、完整预览与下载。
- 视频详情自动请求 `autoplay=1`、唯一 iframe 持久、收起和切维度不卸载。
- 自定义播放列表验证命名、曲库切换、逐行解析、去重、创建/更新、三种模式和存档往返。
- 现实维度 10/20 分钟结算、两种回答、待办 CRUD、明信片背景、苹果钟首次完成只加一天、重复/取消不加天、活力第七日恢复与通知降级。
- DEBUG 全收集包含好友，撤销后收藏与好友均为空。
- 10 Hz 守候音频测试以进入前 baseline 为准，不能把钢琴 oscillator 与守候 oscillator 混为一个。

跨域播放器验收只能断言请求 URL、iframe 生命周期、主动切歌、任务事件和持久状态；不得断言父页面无法观测的真实播放、进度、暂停或结束。
